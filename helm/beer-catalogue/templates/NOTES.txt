Database Secret created

A Kubernetes Secret named "{{ .Values.database.secretName }}" has been created with the following keys:

- url
- username
- password

k8s/deployment.yaml already consumes it as Spring Boot datasource settings:

  env:
    - name: SPRING_DATASOURCE_URL
      valueFrom:
        secretKeyRef:
          name: {{ .Values.database.secretName }}
          key: url
    - name: SPRING_DATASOURCE_USERNAME
      valueFrom:
        secretKeyRef:
          name: {{ .Values.database.secretName }}
          key: username
    - name: SPRING_DATASOURCE_PASSWORD
      valueFrom:
        secretKeyRef:
          name: {{ .Values.database.secretName }}
          key: password

Verify the secret with:
  kubectl get secret {{ .Values.database.secretName }} -o yaml
